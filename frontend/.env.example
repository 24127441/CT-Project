# Frontend environment example (Flutter)
# Keep secrets out of the repo. These are for local/dev only.

# Public/anonymous Supabase key (safe for frontend but rotate if exposed)
SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_URL=https://your-project-id.supabase.co

# Map tile provider key (MapTiler, Mapbox, etc.)
MAPTILER_KEY=your_maptiler_key_here

# Generative API key (e.g., Gemini). Keep this out of public builds if possible.
GEMINI_API_KEY=your_gemini_api_key_here

# Usage notes:
# - Local dev: use `flutter_dotenv` to load this file (add to .gitignore).
# - Production builds: prefer passing values with `--dart-define`:
#   flutter build <platform> --dart-define=SUPABASE_ANON_KEY=xxx --dart-define=MAPTILER_KEY=yyy
# - Web static hosting: inject a runtime `env.js` that sets `window.__ENV` and read it in `index.html`.