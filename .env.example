# Repository-wide example environment variables (development)
# Copy values into appropriate `.env` files (e.g., `backend/.env`, `frontend/.env`) or provide at build time.
# DO NOT commit real secrets to version control.

# -------------------------
# Backend (Django / server)
# -------------------------
SECRET_KEY=your_django_secret_key_here
DEBUG=True

# Database (preferred: DATABASE_URL)
DATABASE_URL=postgresql://postgres:password@db.yourhost.co:5432/postgres
# If you prefer separate vars (optional)
DB_NAME=trekguide_db
DB_USER=postgres
DB_PASSWORD=your_db_password_here
DB_HOST=localhost
DB_PORT=5432

# Email (Django email sending)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_email_app_password

# -------------------------
# Supabase (shared)
# -------------------------
# Server-side: use service role key for seed tasks and server operations
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here
SUPABASE_JWT_SECRET=your_legacy_jwt_secret_here
# Frontend (public) anon key (frontend/.env or build-time)
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# -------------------------
# Frontend (client-side)
# -------------------------
# Map provider API key used for 3D tiles/styles
MAPTILER_KEY=your_maptiler_api_key_here

# Generative model / LLM API key (Gemini/OpenAI)
GEMINI_API_KEY=your_gemini_api_key_here

# -------------------------
# Usage notes
# -------------------------
# - Backend: copy to `backend/.env` and load via python-dotenv or your hosting env.
# - Frontend (Flutter): prefer `--dart-define` for CI/build, or use `flutter_dotenv` for local dev.
#   Example (dart-define):
#     flutter build apk --dart-define=SUPABASE_ANON_KEY=xxx --dart-define=MAPTILER_KEY=yyy
# - Web static `index.html`: inject runtime `window.__ENV` with a small script at deploy time.
# - Rotate any keys found in source; do not store production secrets in the repo.
